// Automatic loader removed — reverting to manual `blogs.json` workflow.
// This file is intentionally a stub so the previous manual-index code path works.

export function getAllBlogs() {
  return [];
}

export function getBlogBySlug() {
  return null;
}
